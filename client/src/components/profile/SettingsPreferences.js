import React from 'react';
import './ProfileTabs.css';

const SettingsPreferences = ({ userData, onUpdate }) => {
    return (
        <div className="settings-preferences-tab">
            <p>Settings & Preferences - Coming soon</p>
        </div>
    );
};

export default SettingsPreferences;
